apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: rollout-bluegreen
spec:
  replicas: 2
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: rollout-bluegreen-blue
  template:
    metadata:
      labels:
        app: rollout-bluegreen-blue
    spec:
      containers:
      - name: rollouts-demo-blue
        image: argoproj/rollouts-demo:blue
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
  strategy:
    blueGreen: 
      activeService: rollout-bluegreen-blue
      autoPromotionEnabled: true 

---
kind: Service
apiVersion: v1
metadata:
  name: rollout-bluegreen-blue
spec:
  selector:
    app: rollout-bluegreen-blue
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rollout-bluegreen-blue
spec:
  rules:
  - host: blue.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: rollout-bluegreen-blue
            port:
              number: 80
